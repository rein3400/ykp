{"action":"evaluate","args":{"code":"(() => { const els = [...document.querySelectorAll('button,a,[role=button],input[type=submit],input[type=button],label')]; return JSON.stringify(els.map((b,i)=>({i,tag:b.tagName,role:b.getAttribute('role'),text:(b.innerText||b.value||b.getAttribute('aria-label')||'').trim().slice(0,60),href:b.getAttribute('href'),type:b.type||null,html:b.outerHTML.slice(0,120)})).filter(x=>x.text).slice(0,40)); })()"},"session":"hr-pilot-test"}
