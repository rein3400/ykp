{"action":"evaluate","args":{"code":"(() => { const links = [...document.querySelectorAll('a')].map(a => ({text: a.innerText.trim(), href: a.getAttribute('href')})); return JSON.stringify(links); })()"},"session":"hr-pilot-test"}
